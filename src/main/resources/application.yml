server:
  port: 8081
spring:
  liquibase:
    enabled: true
  datasource:
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: postgres
    url: jdbc:postgresql://localhost:2023/postgres
  jpa:
    hibernate:
      ddl-auto: update
  h2:
    console:
      enabled: true
